package org.example;

import org.junit.jupiter.api.Test;

class ContractTest {

    @Test
    void writeTestsFromLabMd() {
        // TODO: Write SalesContract and LeaseContract tests using the rules in LAB.md.
        // Contract amounts use BigDecimal. Compare with:
        //   assertEquals(0, expected.compareTo(actual));
        // or assertEquals(expected, actual) when both use the same scale (2 decimal places).
    }
}
